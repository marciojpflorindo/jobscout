# dashboard/

Local web app: a small Python HTTP server bound to `127.0.0.1`, a static front
end (HTML/CSS/JS), and one JSON store on disk. Review found jobs and track every
application through its status lifecycle. No auth — localhost, single user.

_Implemented in Phase 1._
